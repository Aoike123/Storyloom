> 当前采用免账号 Demo，本文历史账号与访问隔离方案已取消；以 [AUTHOR_WORKFLOW.md](AUTHOR_WORKFLOW.md) 为准。

# 互动漫剧工作流：第一版开发计划

> 更新：第一版作为演示原型保留。真实 LLM / 生图 / 生视频接入、赛方故事与标签接入、多故事导演与分镜编排，统一按 [第二版开发计划（待审核）](DEVELOPMENT_PLAN_V2.md) 推进。下文为第一版历史计划，不表示其中所有目标均已交付；实际完成范围见 README.md 和 VERIFICATION.md。

## 目标与范围

制作一个可本地运行的网页原型：读者播放经过审核的原版视频，可以在任意时刻暂停，以自然语言修改当前及后续剧情；系统保留已经发生的事实，生成合理的过渡，并在剧情与视觉条件均匹配后接回作者主线。读者也可以在新生成的片段中再次暂停改写。

不要求读者在预设选择点操作，不以节点地图作为读者界面。后台的事件、时间标注和片段仅用于状态计算与制作管理。

采用现有模型 API，不训练模型。第一版使用一篇原创或获得改编授权的故事、少量人物与场景、一个作者目标结局。原版制作线和读者互动线共用版本化素材库。

“随时提出修改”不等于保证所有修改都能与固定结局兼容。无法在现有事实和世界规则下回归时，呈现冲突及可行改写建议，等待读者选择；不偷偷撤销读者修改，不默认接受直接否定作者必要约束的修改。不承诺即时视频生成或逐帧无缝续接。

## 技术方案

- 前端：Next.js、React、TypeScript；原生 HTML5 Video 播放器。
- 后端：Python、FastAPI、Pydantic；HTTP 提交操作，轮询获取第一版任务进度。
- 数据库：PostgreSQL；业务字段使用关系表，可变剧情状态使用受结构校验的 JSONB。
- 后台执行：独立 Python Worker，使用数据库任务表、原子领取、租约和心跳；第一版不引入 Temporal、Celery 或 LangGraph。
- 文件存储：本地文件与统一存储接口起步，模型需要公网素材时接入对象存储及有时效的读取地址。
- 视频处理：FFmpeg 与 FFprobe，负责检测、抽帧、裁剪、规格统一、字幕音轨合成与导出。
- 模型：语言、生图、生视频 API 分别通过适配器接入；配音按视频模型能力和故事需要决定是否单独接入。
- 运行：数据库以容器方式提供；前端、后端、Worker 可分别启动。启动脚本与环境检查文档随代码交付。

服务端统一持有密钥，前端不得获取密钥。调用模型属于外部操作，任务状态和厂商任务编号必须持久化；视频文件不放入数据库。

## 阶段 0：确定验收故事和项目骨架
工作：
- 建立前后端、Worker、数据库迁移、素材目录与配置样例。
- 创建一个小型原创悬疑样例，明确作者结局、必要事实、可修改范围；可随后替换成用户提供的授权故事。
- 建立统一错误格式、任务日志、供应商接口和模拟适配器。
- 提供启动检查与基础健康检查。

交付与验收：本地页面可打开，后端和数据库连通；样例故事、人物与场景可查看。模拟调用仅用于验证流程，并在页面明确标注，不作为真实 AI 完成结果。

用户输入：可提供自己的故事和偏好画风；没有时先使用原创样例，不阻塞基础建设。

## 阶段 1：任务系统、素材库和 API 配置入口

工作：
- 建立故事、版本、角色、场景、素材、审核记录和后台任务数据结构。
- 素材状态分为草稿、待审核、通过、退回；读者修改副本与公共库隔离。
- 建立本地密钥配置方式及连接检查页面，页面只显示是否已配置与脱敏状态。
- 任务采用原子领取、租约续期、有限重试、重启恢复和预算检查。
- 保存提交意图、去重标识和厂商任务编号。提交结果不明确时进入待核实状态；供应商不支持去重或查询核实时，不盲目重复提交。

交付与验收：素材可上传、版本化与审核；两个 Worker 不重复领取同一有效任务；中断恢复不会重复执行已确认完成的步骤。

API 输入点 A：语言模型
- 到本阶段完成后，提示用户选择供应商，并提供官方接口地址或文档、模型 ID、调用方式和测试额度。
- API Key 由用户写入本地未纳入版本控制的 `.env.local`；提供无真实密钥的 `.env.example` 和填写说明。
- 不要求在聊天中发送密钥，不将完整密钥写入日志、截图或返回前端。
- 告知测试范围后，按用户确认的调用数量或费用上限执行真实连接测试。

## 阶段 2：先验证文字层的随时改写与回归

工作：
- 原文结构化：事件、人物知识、物品、关系、作者约束和后续情节条件。
- 剧情引擎分为修改解释、候选状态更新、回归规划、因果检查四步。
- 使用事件记录和快照重建状态；模型输出先做格式校验，再做规则与语义检查，通过后才提交为新状态。
- 回归计划以作者主线条件为目标，保留读者已造成的事实；近期片段逐步执行，后续计划可被新的输入替代。
- 每次修改创建分支修订号，保存父版本和输入位置；拒绝过期请求或提示基于最新版本重新提交。
- 人物/场景修改区分外观变化与影响剧情的变化，后者同时重算回归可行性。

交付与验收：同一故事可在多个时间位置提出自由文本改写，并连续改写两次；返回可解释的过渡计划。与结局冲突的修改有明确处理，不凭空恢复伤势、物品或人物记忆。

重要限制：结构校验不代表剧情正确。使用人工标注样例检查语义评价器，记录误判；达到重试上限后停止并展示问题。

## 阶段 3：接入真实视觉 API，完成原版制作线

API 输入点 B：生图、生视频及必要的配音
- 索取供应商名称、官方文档、模型 ID、已开通权限；密钥继续由用户本地填写。
- 核实文字/图片输入、参考图数量、时长、画幅、声音、上传方式、状态查询、取消和计费能力。
- 用户配置首次测试范围、项目额度、并发和最大重试次数；费用无法预估时先以调用数和生成秒数控制，不显示虚构报价。
- 图片输入需要公网地址且供应商无文件上传入口时，再提示配置对象存储；确认后测试一张参考图、一个短视频。

工作：
- 接入一家语言模型、一家生图和一家生视频供应商，允许按能力共享供应商。
- 生成剧本、角色场景参考素材、分镜与粗剪；审核通过后制作原版视频片段。
- 对实际成片建立时间标注：事件生效时刻、动作区间、台词、片段前后状态；辅助自动标注后允许人工修正。
- 原版素材审核与成片审核分别记录；发布时绑定故事、素材、片段及标注版本。
- 统一媒体规格并保存到自己的存储；素材权限与临时读取地址由后端管理。

交付与验收：完成一段建议约 60—90 秒、包含多个片段的真实原版视频，审核后可播放；时间标注与画面一致。单个镜头可重做，已有通过版本保持可用。

## 阶段 4：任意暂停、生成过渡与接回原版

工作：
- 播放器随时接受暂停改写，提交播放列表版本、片段版本、片段内时间和改写内容。
- 以成片时间标注重建暂停状态，结合暂停帧处理动作进行中的位置；无法判定的状态不凭单帧臆断，进行补充检查或明确澄清。
- 默认保留暂停前已发生事件；要求改写既成事实时，明确提供回退到相关时刻的操作，不静默重写过去。
- 文字计划先通过检查，再选择人物场景素材，制作过渡分镜和视频。
- 暂停前已观看画面保留，之后替换为新分支片段；切口按实际时间裁剪/重编码，必要时使用有动机的切镜。
- 同时检查剧情与视觉接入条件，匹配后复用原版后续；不匹配则继续过渡或重做受影响部分。
- 新生成片段也建立时间标注与状态，支持在其中再次暂停修改。
- 每个结果绑定分支修订号；旧任务尽力取消，取消不受支持时保存为过期结果，不覆盖当前播放列表。

交付与验收：用户可在镜头中间改写、在过渡片段再次改写，最终接回符合条件的作者主线；旧生成任务晚到不会污染新剧情。

## 阶段 5：审核、局部修复和成本控制

工作：
- 自动检查覆盖文件可用性、音轨字幕时长、资产版本、剧情与视觉连续性。
- 问题绑定到具体片段和事实；修复仅重做受影响部分。
- 原版仍保留素材与成片人工审核入口；读者分支采用自动检查，结果不确定时进入待处理，不伪称经过人工审核。
- 根据输入变化计算受影响范围；复用素材或片段前验证依赖，不能只按相同提示词复用。
- 保存实际调用次数、生成秒数、供应商返回的用量及可获取的费用；明确区分估算和实际账单。
- 设置任务超时、有限重试、预算暂停和人工接管入口。

交付与验收：失败片段可定位、重试和人工处理；达到上限自动暂停；未通过必要检查的片段不进入正式播放列表。

## 阶段 6：集成验证与参赛演示

必须验证：
1. 原版审核通过后才能发布，发布引用的版本稳定。
2. 镜头开头、中间、对白途中、动作途中均能提交改写。
3. 状态只包含已发生事实，未播放后续不提前进入历史。
4. 玩家夺刀等成功修改被后续记住，桥接不直接撤销它。
5. 过渡视频中连续改写，旧任务结果不覆盖新分支。
6. 回归时人物知识、伤势、服装、物品和场景条件一致。
7. 无合理回归方案时明确解释并提供选择，不强行生成。
8. Worker 重启、API 超时、素材链接失效均有恢复或清晰失败状态。
9. 密钥不进入前端、日志和版本控制；预算与重试限制有效。

测试方式：纯逻辑与模拟供应商覆盖规则、版本竞争和恢复；真实 API 只按确认额度执行有代表性的端到端测试。避免每次代码修改都重新生成收费视频。

最终交付：可运行代码、数据库迁移、配置样例、启动说明、原创演示故事、真实素材与视频、演示操作步骤，以及实测耗时/成本/人工干预记录和已知限制。

## 执行原则

- 先证明文字层的合理桥接，再投入批量视频成本。
- 模型供应商确定后核对当时官方文档，不使用虚构接口。
- 基础界面与模拟流程可独立推进；缺少密钥时不把模拟结果宣称为真实完成。
- 真实密钥和付费测试额度是明确的用户输入点；等待期间继续完成不依赖它们的实现。
- 开发周期在首次 API 测试后估算，依据实际等待时间、成片通过率和返工量调整。
- 第一版不包含多人实时共创、任意故事全自动适配、模型训练、无限分支预生成和零等待视频承诺。

## 2026-09-12 视觉一致性流程（已实现）
- 统一视觉规范、逐镜1–3张审核参考图、连续性状态及三个小样镜头配置。
- 使用硅基流动 Qwen/Qwen-Image-Edit-2509 三图参考协议；不改变探索生图模型配置。
- 小样审核通过前，限制为选定三个镜头；禁止其余镜头、视频和发布。
- 参考版本变化或小样重做/退回导致审核失效，历史素材保留；新镜头须重新生成。
- 视频使用当前审核关键帧；发布检查对应版本。人工审核仍负责人物、场景与画风判断。
- 参考编辑图单独设置计价，不复用 Z-Image 单价。
- 离线验证：54项测试通过，TypeScript检查通过；浏览器确认新设定面板与门槛已显示。
- 未进行收费模型实测；需用户确认账号可调用编辑模型，并完成三镜效果验收。
- 参考图制作/上传沿用人物与场景资产库及模型连接中的探索生图入口。自动人物设定提取、自动视觉判别和读者实时分支生成尚未包含在本次改动中。

## 2026-09-12 读者与管理员产品边界

已实现：
- `/` 为读者独立页面，不加载管理轮询、管理组件或生成入口。
- `/admin` 为独立登录的管理入口；移除两端切换按钮。
- 后端管理读写接口验证管理员会话；未发布素材禁止匿名读取。
- 读者接口仅提供已发布作品必要字段；支持 `/?story=发布ID` 直接打开作品。
- 本地首次启动生成 `data/admin-access.txt` 管理口令；不在页面或日志显示。会话有效8小时，退出立即清除浏览器会话。
- 56项测试及前端类型检查通过。

尚未完成的读者核心体验（不可用“记录想法”代替）：
1. 知乎微小说到对应已发布漫剧的真实入口接入；当前仅具备本站作品直达能力，未修改知乎客户端。
2. 每位读者独立的观看会话和分支版本，不能读写他人分支或管理员原版。
3. 暂停位置的已发生事实快照；修改当前与未来事件，保留此前历史。
4. 因果规划、作者主线回归约束及无法桥接时的明确反馈；禁止强行突变回归。
5. 复用批准的视觉资产生成个人分支，提供进度并在完成后衔接续播；不覆盖发布原版。
6. 管理员批量制作基础版本，发布审核后的不可变作品；读者不承担导演、素材审核或模型配置操作。

本轮没有发起收费生成。当前登录方案面向本地单管理员；对外部署仍需 HTTPS、独立管理域名和正式账号/读者会话体系。

## 2026-09-12 废案归档与重做
- 导演执行台增加当前方案选择、归档原因、归档列表、旧分镜及历史素材查看。
- 归档保留方案、任务、素材及费用记录；不撤回已发布作品。
- 有排队、运行或等待中的关联任务时阻止归档，提示先处理任务。
- 归档方案不能继续编辑、批准、生成或发布；支持恢复，恢复后视觉版本需重新审核。
- 从归档方案准备重做时继承原创作要求供修改，明确确认收费后才新建独立导演项目；保存来源关联，不继承旧镜头和旧审核。
- 验证：58项测试通过，前端类型检查通过；未发起收费生成。

## 2026-09-12 选角与搭影棚前置（已实现）
新建导演任务只生成导演阐述（一次 LLM）。随后必须完成角色、影棚、道具参考资产选择与设定、分组定装试拍和人工审核，再单独启动正式分镜及预审（最多两次 LLM）。
- 角色设定、场景布局、灯光与统一画风写入版本快照；试拍必须覆盖全部选定资产。
- 试拍使用已有 Qwen 参考编辑适配器，每组2–3张参考，必须同时包含演员与影棚。
- 正式分镜只能引用选定资产ID，并包含场景资产；不符合绑定要求时保存诊断并停止。
- 分镜后自动继承画风、资产绑定和连续性描述，仍执行三镜小样审核。
- 修改参考版本使后续审核失效；重做分镜保留旧分镜历史和媒体任务。
- 归档旧案可载入演员与影棚设定，重新核对并审核试拍；不自动发起付费请求。
- 旧方案及已发布素材未迁移或删除；已有导演阐述可直接进入新的前期准备，再重做分镜。
- 62项测试及 TypeScript 检查通过；浏览器确认前期准备面板和分镜门槛。未进行收费画面实测。
- 当前选角搭景通过人工资产选择、文字空间说明和参考图完成，尚无可视化三维影棚编辑器或自动角色设定生成。
