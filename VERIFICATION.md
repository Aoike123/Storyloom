# 第一版验收记录

日期：2026-09-12

## 已运行的检查

- 后端：22 项自动化测试通过（`python -m pytest -q`）。
- 前端：Next.js 正式构建通过，包含 TypeScript 检查。
- 本地前端、后端与独立 Worker 已启动；只监听本机地址。
- 数据库采用 SQLite 本地模式。

## 浏览器实际操作

1. 原版第二段的第 2.35 秒（整条时间线第 22.35 秒）暂停。
2. 提交“林夏侧身躲开，夺下歹徒的刀”。
3. 演示规则生成过渡方案后，Worker 自动制作片段并更新播放列表，无需再次点击制作。
4. 在生成的夺刀片段第 7.95 秒暂停，状态显示“林夏持有”刀具，证据仍为“尚未发现”。
5. 在该分支再次提交求助，第二轮自动制作完成。
6. 验证片段间自动续播。
7. 新建“林夏 · 蓝色风衣测试副本”，搜索到私有副本，执行审核；原始米白风衣角色未被覆盖。
8. 导出当前播放列表，无声示意版 MP4 成功生成并显示下载入口。

浏览器测试数据保留在本地，名称已注明测试用途。未调用真实模型，真实 API 提交次数为 0。

## 自动化测试重点

- 暂停位置重建不泄漏未来事件。
- 修改冲突、未定义状态与不合理跳步会被阻止。
- 连续改写与过期请求隔离。
- 旧任务、已取消任务不得覆盖新播放列表。
- 两个 Worker 对同一任务只允许一个成功领取。
- 收费提交状态不确定时不自动重发；恢复要求已有供应商任务编号。
- 停止真实过渡会停止尚未提交的子镜头。
- 真实过渡在标注、审核与接入条件满足前不能提交。
- 已引用的真实片段标注和审核状态被锁定。
- 密钥不在配置接口返回；跨来源浏览器写入被拒绝。

## 未验证与边界

- 尚无用户供应商配置与测试额度，真实语言/视频适配器没有使用真实账号调用。真实画质、费用、延迟和叙事质量未经验证。
- 生图、配音专用接入待供应商选择；当前支持上传图片，示意动画由程序绘制。
- PostgreSQL 配置已预留，但此环境未运行 PostgreSQL 集成测试。
- 当前是一个原创故事的垂直原型，尚不是任意小说全自动适配器。
- 自动语义检查不等于可靠的人类判断，真实视频仍需核对事件时间与首尾视觉衔接。
- 没有多用户身份与数据隔离，不对公网部署。
- 测试框架有两条依赖弃用提示；不影响本次测试结果。
