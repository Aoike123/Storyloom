# 脑洞微小说目录与漫剧制作流程

当前有两个主要入口：读者目录与漫剧制作。所有生成都必须有付费身份——登录知乎账号领取算力豆，或在账号面板填写自己的 Key；没有任何付费身份的浏览器只能浏览，不能生成。公网演示下作品按账号隔离。原管理页面已合并到制作页。

## 本次实现

- 分镜存储升级：新生成/上传的视频保留原文件并登记播放版本、文件摘要和媒体实测属性；角色/场景/关键帧保存生成时图片快照。审片区间对齐实际视频帧，生成独立选用记录及入画/出画图片；发布生成版本化清单，旧视频与旧发布继续兼容。详见 [存储规范 v1](docs/video-storage-format-v1.md)。

- 读者入口 `/`：读取知乎公开故事列表，完整展示本次返回的脑洞类作品，支持搜索和状态筛选。已发布且视频文件存在的漫剧置顶，其余卡片置灰但保留“查看微小说，生成漫剧”入口。列表接口没有分页参数，不能将返回目录等同于知乎全站故事库。
- 制作入口 `/author?story=<知乎 work_id>`：只读取所选微小说正文，保留来源与署名，自动建立或继续该故事的制作记录。不再提供新建小说表单或手动提交小说接口，打开原文不会触发收费任务。AI 推荐 3–5 个风格或自由描述画风，确认图片后自动制作视频，最后审片发布。已有制作可通过 `/author?work=<制作 ID>` 继续。
- 原 `/admin` 自动转到 `/author`。上一轮三步简化已回退。制作步骤恢复后，将原制作节点拆为“分镜生成 → 漫剧生成”，每个节点独立保存进度、结果与失败信息。
- 作者端的创作摘要和方案说明连续逐字呈现实际返回内容，不因新数据而从头重播；完成与错误提示立即可读。读者端使用快速因果规划和已有参考资产生成轻量分支，逐段就绪并预加载下一段。界面不模拟模型生成百分比，详见 [制作页简化与文字呈现](docs/simple-author-flow.md)。
- 读者分支不创建新场景、人物或着装资产；简单改写默认合并为单镜，必要时普通改写最多两镜、末尾因果收束最多三镜。多段由两个专用通道并行生成；分支后禁止跳到未观看进度，没有可信回归点时不强行接回作者结局。
- 已移除注册、登录、退出、管理员口令、账号会话和作品归属校验。旧版本账号与会话记录、管理口令文件在启动时清理；保留原文署名、作品、素材与生成记录。
- 图片满意确认和成片审片发布仍是核心制作步骤。读者目录中只有已发布且可播放的作品标为“已有漫剧”；未发布媒体也可在演示环境直接访问。
- 新制作使用 `author-brainstorm-v7`；`v1`–`v6` 线路（含定装合成、试拍、镜头参考图）已删除，代码与用例不再保留。画风、身份、服装、场景、提示词、剧情、分镜与审片分别绑定专业 Skill，配置和实际调用可在制作页检查。详见 [节点绑定](docs/node-skill-bindings.md)。
- `author_flow` 在数据库持久化并由现有 worker 执行，前端关闭不影响调度。中断的执行不盲目重放；视频保留供应商任务号，复用已有查询。
- 当前制作节点或其子任务报错时，页面提供“重新运行当前节点”；新任务保留旧诊断和已完成素材，并携带上次错误修正。可安全复用保存结果时仍可选择“继续当前节点”。
- 图片阶段由作者确认；分镜先完整绑定人物身份图、独立服装图与场景图。单镜不超过三张时保持图片分离；只有超过三张时，才把该镜所需的同一人物身份图与服装图做本地双栏拼接，不调用 AI 融合，也不改动原图；拼接后仍超限才要求拆镜。后续中间关口按自动工作流继续，审计明确区分自动接受与人工审核。自动接受只代表结构/引用/资产版本约束，不能代表视觉质量合格。
- 作者按作品查看用量；管理员计价和全平台用量保持独立。没有新增作者提交次数额度。

## 尚未完成，不能当作已交付能力

1. 完整微小说自动拆集和全篇覆盖：当前导演仍为 4–8 镜短场景。
2. 独立配音、口型、音乐混音与完整单文件成片；当前为真实视频按剪辑区间连续播放，非图片轮播。
3. 多模态视觉质检与自动局部修复；当前为文本预审、资产引用/版本检查以及作者验收。
4. 读者分支的多模态剧情命中检查、自动视觉质检和局部修复；当前只有结构约束、素材锁定与媒体技术检查。
5. 向知乎实际发布尚未接入。已接入公开脑洞故事目录选择，当前漫剧只发布到本地读者空间；账号、身份认证和 OAuth 不属于此 Demo 的开发目标。
6. 工作流模块编辑/灰度发布尚未实现；目前沿用持久化 FIFO worker。多账号调度不属于此 Demo 的开发目标。
7. 完整付费调用的网络故障恢复和跨请求幂等性：已有任务与媒体保留，调度恢复不等于可任意重放收费步骤。

## 命令行启动

推荐一键启动前端、后端与执行器：

```powershell
cd C:\Code\知乎AI竞赛
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\start.ps1 -Dev
```

读者目录：`http://127.0.0.1:3000/`；漫剧制作：`http://127.0.0.1:3000/author`。原 `/admin` 转到制作页，无需账户或口令。

也可打开三个 PowerShell 终端分别启动。前两个在项目根目录执行：

```powershell
cd C:\Code\知乎AI竞赛
.\.venv\Scripts\python.exe -m uvicorn backend.app:app --host 127.0.0.1 --port 8000
```

```powershell
cd C:\Code\知乎AI竞赛
.\.venv\Scripts\python.exe -m backend.worker
```

第三个启动前端：

```powershell
cd C:\Code\知乎AI竞赛\web
pnpm dev
```

已有服务时先在原终端 Ctrl+C，再启动新代码；不要同时运行多个旧执行器。启动 worker 会继续已有排队任务。

## 验证

自动化测试使用独立数据库，覆盖无需 Cookie 的三端接口与管理操作、跨浏览器共享作品及未发布素材、移除旧登录接口、旧账号数据清理和作品保留。同时保留草稿不调用模型、图片确认前不制作视频、生成完成等待发布、重复发布不重复创建、中断任务不盲目重放等检查。

本轮不发起收费模型任务。真实画质、声音与连贯性仍需制作样片实际验收。

本轮验证：76 项自动化测试通过，TypeScript 检查通过。新增检查覆盖全量脑洞筛选、按来源标识匹配并置顶已发布漫剧、缺失视频不标记可播放、缓存与离线目录、原文与署名保留、重复进入继续已有制作、不自动调用收费模型。当前使用开发模式。

浏览器验证通过：真实目录的 10 篇脑洞卡片、置灰制作入口、原文与署名展示、圆点连线步骤、重复访问继续同一制作、原 /admin 跳转，以及用临时样例验证已发布置顶、状态筛选与视频加载。临时发布样例已清理，保留首篇微小说的原文制作记录，尚未触发付费生成。
