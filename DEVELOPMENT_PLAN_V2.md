> 当前三端采用免账号 Demo，所有操作共享开放。最新制作流程与待完成事项见 [AUTHOR_WORKFLOW.md](AUTHOR_WORKFLOW.md)。

# 叙间第二版：真实模型、知乎故事与按标签导演编排

状态：用户已批准，正在逐项实施；以下验收条件仍需完成，不能视为已交付。

供应商选择（2026-09-12）：LLM 使用 DeepSeek；视频使用官方 MiniMax-H3-Max V2；生图待定，Z-Image 为候选。生图托管服务和具体 DeepSeek 模型 ID 待配置。H3 Max 支持 5–15 秒、480P/768P、首尾帧输入，不支持参考图/视频/音频混合输入；人物库需先参与关键帧制作。已开始增加协议适配及离线协议测试，尚未进行收费调用。既有本地密钥配置不覆盖。

实施进度：已增加 DeepSeek、MiniMax H3 V2 协议适配、网页本地密钥配置、硅基流动文生图适配与关键帧审核后生成视频的入口。生图预设为 Tongyi-MAI/Z-Image-Turbo，用户可修改模型和区域地址。已有 34 项离线测试通过，包含生图入库、审核门槛、本地首帧传递及中断保护。尚未使用真实密钥调用收费模型，整篇故事自动生产尚未完成；知乎故事库已接入 Skill 提供的公开 HTTP 接口，支持列表、标签筛选、按需正文读取、缓存和来源版本导入；按标签导演仍待实施。

## 1. 本版必须实现的结果

用户在故事库中选择赛方提供的真实故事，系统读取原始标签和可用正文，检查内容完整性，生成适合该故事的导演方案与分镜流程。通过真实 LLM、生图、生视频 API 制作原版；读者可在任意时间暂停改写，由系统结合该故事的规则与导演方案生成过渡，合理接回作者主线。

两条线路保留：

- **原版制作线**：故事选择 → 来源与完整性检查 → 故事分析 → 标签路由 → 导演计划 → 分镜 → 人物/场景/关键帧生图 → 素材审核 → 视频生成 → 成片与时间标注审核 → 发布内部原版版本。
- **读者互动线**：任意暂停 → 重建当前状态与暂停画面 → 理解修改 → 按原导演方案规划过渡 → 检查因果和回归可行性 → 复用或生成关键帧 → 生成必要视频 → 检查衔接 → 接回可复用的原版后续。

读者不需要选择预设剧情节点。内部时间标注、状态检查点和片段边界仅用于计算，不限制改写发生的时刻。允许在生成后的过渡中再次改写。

不将“任何修改必然合理回归”作为无条件承诺：与作者必要事实矛盾或没有可信回归路径时，明确提示冲突和可行调整，不撤销已接受的事实。

## 2. 当前代码的真实缺口

| 当前实现 | 本版改造 |
|---|---|
| `providers.py` 只有有限的 Chat Completions JSON 及方舟视频 Tasks 请求 | 按真实供应商协议实现、测试并登记能力；区分配置存在、连接成功、能力验证、端到端验证 |
| 没有生图调用，只有上传图片与示意素材 | 增加真实文生图、供应商支持的参考图编辑/变体、关键帧生成和素材入库 |
| `domain.py` 写死 INITIAL、ALLOWED、JOIN_STATE | 改为每个故事自己的实体、事件、状态模式、世界规则、作者主线与回归条件 |
| `app.py` 固定 story_demo；回归固定 clip_end | 会话显式绑定 story_id、原版版本、导演模板版本和播放列表版本 |
| `worker.py` 视频提示词写死林夏、米白风衣与站台 | 提示词引用故事资产与镜头规格，按故事/分镜传递参考素材 |
| 图片副本不会进入生成任务 | 增加资产绑定、版本快照、修改影响分析与过期结果隔离 |
| `auto_render` 只用于演示规则生成示意动画 | 加入真实制作的可恢复工作流与审核等待，在用户授权额度内自动继续 |
| 原版文字、状态提示和部分页面围绕单个示例 | 改为真实故事库、故事工作台、导演方案与各故事阅读会话 |
| 当前导出为无声示意视频 | 保留真实音轨，按需要接入配音；统一画面、音轨、字幕与最终导出 |

因此，本版验收不能用“填了 Key”“返回了任务编号”或“演示规则跑通”代替真实模型闭环。

## 3. 赛方接口已核对的信息

来源：本机 `zhihu/SKILL.md`、`references/hackathon.md`、`references/hackathon-content-api.md`；Skill 版本为 `0.5.3-beta.20260904115023`。

| 能力 | 官方接口 |
|---|---|
| 故事列表 | `GET https://api.zhihu.com/km-indep-home/hackathon/v2/story/list` |
| 故事详情 | `GET https://api.zhihu.com/km-indep-home/hackathon/v2/story/{work_id}` |
| 标签来源 | 上述列表和详情中的 `labels` 数组；所读文档没有单独列出标签接口 |

本次最小实测：列表 HTTP 200，返回 20 条；选取列表返回的一条 ID 请求详情，HTTP 200。详情包含 work_id、chapter_name、author_avatar、author_name、labels、introduction、content。元数据证据见 [API 核对记录](docs/zhihu-api-verification.json)。这只是当前响应规模，不推断接口永远只有 20 条或支持未文档化的分页、标签参数。

实际多标签示例：
- 《近视眼勇闯恐怖游戏》：惊悚、脑洞、无限流、甜宠、爽文、现代。
- 《西游之众佛腐烂》：悬疑、玄幻奇幻、脑洞、西游、克苏鲁、架空。
- 《李冬原著：同时被两个精神病追杀》：悬疑、惊悚、脑洞、病娇、犯罪、求生、反转、现代。

这些接口无需 Access Secret 或 OAuth，也不通过当前 CLI。Skill 状态检查显示 CLI 尚未安装；应用接入这里的公开 HTTP 接口不依赖安装 CLI。本版不加入知乎账号登录或个人数据读取。

### 故事库实现要求

- 后端请求固定的官方域名；前端只调用自己的故事库接口。
- 列表缓存、按需刷新，选定故事后才获取详情；不后台循环抓取全部正文。
- 由已获取的列表确认 ID，拒绝路径分隔符、查询片段、回车换行，并做 URL 路径编码。
- 保留作者、原始标签、来源接口、获取时间、正文散列和源版本；缺失字段显示缺失，不补造。
- 标签筛选先对已返回列表在本地进行；保留原始标签，标准化别名与派生导演标签另存。
- 将原文和标签作为数据，不执行正文中夹带的指令；显示时进行内容清理，避免将外部 HTML 直接作为可信界面执行。
- 接口失败时显示失败；存在缓存时明确显示缓存时间，不能悄悄换成演示故事。
- 接口只面向该次黑客松；使用范围按赛方说明核对，不将公开可读等同于无限改编或分发授权。

### 必须增加正文完整性检查

本次读取的一条详情 `content` 长度为 3,000 字符。长度本身不证明内容截断，也不证明完整。

- 检查导语、正文终止位置、章节信息、结局是否实际存在；不能仅依赖字数判断。
- 区分“已确认原文完整”“仅有节选/无法确认完整”。
- 无法确认完整时，可制作所提供片段的样片，但不声称已经提取作者完整结局。
- 只有取得包含结局的授权正文，或用户明确提供作者主线约束后，才启用对应的固定结局回归；用户补充与原文提取分别标明来源。

## 4. 标签驱动的是制作流程，不只是提示词里的风格词

建立版本化的导演模板，每个模板包含：故事分析步骤、必要检查、分镜规则、资产规格、生成依赖、镜头预算、回归检查和可用降级方案。

### 4.1 路由顺序

1. 读取原始 `labels`，识别题材、时代、叙事机制、情绪与结局倾向。
2. LLM 对照实际正文判断标签在本故事中的作用，输出原文依据和不确定项。
3. 选择一个主要叙事流程，再组合少量修饰模块；不为所有标签排列组合新建无限流程。
4. 程序校验模板兼容性、阶段依赖、模型能力和预算，保存导演计划。
5. 确认后的导演计划在原版和读者分支中共用；重要变更创建新版本。

优先级：原文事实与作者约束 > 已发布视觉设定和已发生事件 > 叙事流程 > 标签风格建议。HE/BE、现代/古代等标签不能单独决定事实或擅自改结局。矛盾或不明确时产生待确认项。

### 4.2 第一批导演流程（基于真实标签设计，以下规则属于我们的产品方案）

| 模板 | 常见触发标签 | 专有步骤 | 分镜侧重点 | 专项检查 |
|---|---|---|---|---|
| 悬疑与惊悚 | 悬疑、惊悚、犯罪、烧脑、克苏鲁 | 线索整理 → 人物/观众知情时间表 → 信息揭晓安排 → 分镜 | 环境线索、遮挡、反应镜头、关键物品特写 | 是否提前泄底；线索出现与角色知情是否有依据 |
| 情感与关系 | 言情、甜宠、治愈、虐恋、暗恋、先虐后甜 | 关系初态 → 情绪转折 → 对白与潜台词 → 分镜 | 双人位置、视线、表情、停顿与反应 | 关系变化是否突兀；情绪与对白是否一致 |
| 幻想与规则世界 | 玄幻奇幻、仙侠、穿越、重生、无限流、系统 | 世界规则 → 能力/资源/时间线 → 动作分解 → 分镜 | 场景建立、动作空间、技能前提和变化过程 | 是否临时增加能力；是否违反世界规则或因果顺序 |
| 喜剧与反转 | 沙雕、脑洞、反转、多视角反转、打脸 | 铺垫与误导 → 反转点 → 笑点/反应节拍 → 分镜 | 信息差、停顿、视角切换、反应镜头 | 反转依据是否保留；镜头是否提前暴露包袱 |
| 现实与成长 | 现实情感、家庭、职场、励志、校园、草根 | 处境与目标 → 行动阻力 → 选择后果 → 分镜 | 对话调度、物件细节、行动与环境关系 | 动机是否成立；环境与身份是否连贯 |

其他实际标签如末日、求生、丧尸、科幻、动物主角、大女主、古代、现代，以危险/资源、时代美术、角色形态和角色主动性等修饰模块接入。未识别标签保留原值，采用中性基础流程并标注不确定性。

首轮先实现所有模板定义与路由测试；真实收费验证优先挑选两个正文可用、制作差异明显的故事流程。其他模板在完成真实样片验证前标记为未实测，不能把配置存在说成质量已验证。

### 4.3 多标签实例

对于“惊悚＋脑洞＋无限流＋甜宠＋爽文＋现代”，先从正文判断主要冲突。可形成“惊悚主流程＋无限流规则检查＋关系表达＋现代美术”，但不能让甜宠标签消除已成立的危险规则。路由结果会显示选择原因、引用的文本范围、执行步骤与模板版本。

读者改变当前行为时沿用同一导演方案：悬疑要检查是否泄露线索，幻想要检查能力条件，关系故事要检查人物动机。让分支的节奏与镜头语言接得上原版。

## 5. 三类真实模型接入

### 5.1 通用模型配置与状态

- 配置模型档案：供应商、接口协议、区域、模型 ID、能力、超时、并发与费用信息来源。
- 支持 LLM、生图、生视频分别配置，允许同一供应商，但不假设三个接口使用相同协议。
- 密钥在本地环境文件或服务器安全存储中填写，只返回脱敏状态；不让用户把密钥发到聊天中。
- 检查 JSON 模式、同步/异步、参考图数量、首尾帧、视频时长、画幅、声音、取消与输出有效期等实际能力。
- 四个独立状态：未配置 → 已配置 → 最小请求验证通过 → 真实业务闭环验证通过。错误展示明确原因，不自动降级成演示并伪称成功。
- 正式调用前冻结模型档案版本和已确认的项目额度；后续更换配置不改变正在执行任务的查询目标。

### 5.2 LLM

- 故事分析、导演路由、剧本改编、分镜计划、回归规划与辅助检查分别定义结构化输出。
- 按供应商实现 JSON Schema / JSON 模式等能力，不硬性假设所有接口支持同一参数。
- 长文本按事件/章节切分，汇总人物、线索、时间线和规则；生成内容关联原文范围，不只靠整段摘要。
- 模型输出只有通过格式、领域规则和因果检查后才进入下游；不直接把自由文本当作已发生事实。
- 将当前单故事的状态取值限制替换为故事级状态模式；保留版本、事件日志和快照。

### 5.3 生图

- 真实生成角色设定图、场景图、重要道具和分镜关键帧。
- 接入供应商实际支持的参考图编辑/变体；不支持时明确能力缺失，不宣称自动保证一致性。
- 资产生成后进入库，记录提示词、模型、来源任务、版本、参考关系和审核状态。
- 审核通过的设定作为后续关键帧和视频输入；不把每个镜头当作独立的无参考生成。
- 支持用户修改许可字段：外观修改重新生成相关资产；出口、物品、能力等剧情修改同时触发状态与回归检查。
- 稳定处理返回 URL、二进制或 Base64，下载到自己的存储，并核对格式、大小和像素尺寸。

### 5.4 生视频

- 至少接通一家真实供应商的文生视频和图生视频；实际支持的首尾帧/多参考能力按模型档案使用。
- 分镜提示词由导演模板、故事状态、角色场景资产和镜头动作构成，不含演示故事硬编码。
- 保存提交意图、供应商任务编号、模型版本、输入资产版本、用量与输出；超时先核实，避免重复收费。
- 真实分支首镜头使用暂停帧或经修复的关键帧作为衔接素材；镜头之间传递必要的参考，无法自动保证连续时进入审核。
- 支持可靠的任务查询、下载恢复、取消与过期任务隔离；对已经提交的供应商任务，不承诺取消一定能退款。
- 核对实际成片时长与音轨；支持原生声音或配音分支，导出不得静默丢掉真实声音。

## 6. 多故事、资产与回归模型

建议新增或明确分离的数据对象：

| 对象 | 关键记录 |
|---|---|
| 故事来源版本 | work_id、作者、原始标签、正文快照/散列、完整性、获取时间 |
| 故事设定版本 | 实体、世界规则、人物知情、关系、事件、作者主线与结局依据 |
| 导演模板/导演计划 | 模板版本、路由依据、阶段依赖、检查项、镜头规格与预算 |
| 镜头计划版本 | 原文范围、动作、台词、机位、事件区间、输入/输出状态和素材依赖 |
| 素材版本与绑定 | 角色/场景/道具/关键帧、供应商信息、审核、归属与影响范围 |
| 原版发布版本 | 已确认镜头、时间标注、音轨字幕、资产与导演方案的固定引用 |
| 读者会话与分支 | 故事版本、暂停位置、已接受修改、状态快照、回归计划、播放版本 |
| 工作流与审核 | 阶段结果、依赖、租约、提交编号、费用/次数、退回理由与修复记录 |

去除 `story_demo`、`clip_end`、固定角色与固定 JOIN_STATE 的业务依赖。每个故事/原版版本提供自己的可衔接片段与前置条件。使用数据库迁移管理结构变化，保留现有示例与历史记录。

接回原版要分别检查：
- **剧情兼容**：人物知识、存亡、关系、证据、物品、世界规则。
- **视觉兼容**：地点、机位衔接、人物位置、服装、伤势、道具与光线。
- **时间兼容**：动作与对白已完成到哪里，是否跨越未发生事件。

故事前进的事实与角色记忆不能因为素材复用而清空。回归主线不要求抹掉玩家全部影响，只要求满足下一段的必要条件；原版片段不能兼容时生成补充片段或选择更后的位置，不强行拼接。

## 7. 自动化、审核与修复

- 延续 FastAPI、Python Worker、PostgreSQL/本地 SQLite、FFmpeg；本版不为这些功能单独引入 Temporal。
- 以可持久化的工作流阶段和依赖管理运行，而非只在前端串按钮；关闭页面后仍继续已授权任务。
- 原版保留必要的素材与成片审核。审核通过后自动继续下游，不要求反复手动复制提示词或上传同一素材。
- 读者线先检查文字方案，再生成关键帧与视频，优先复用已通过的资产。
- 自动检查分为媒体格式、时间/状态规则、模型辅助视觉/语义检查；辅助模型不确定时转人工，不假装已经审核通过。
- 修复必须针对问题改变输入：参考不足补参考，动作过复杂拆镜，时间不足调整对白/镜头；不会无限原样重抽。
- 资产或剧情变化只使受影响的下游任务过期，已审核原版版本保持不变。
- 按用户选择设定项目总调用上限、视频秒数与重试上限；供应商费用可核实时显示估算和实际用量，无法核实时明确未知，不编造金额。

## 8. 页面变更

1. **故事库**：真实故事列表、原始标签筛选、作者与来源、正文可用性、导入制作。
2. **故事工作台**：正文与主线依据、导演方案、模板选择原因、可编辑参数和执行阶段。
3. **模型连接**：三类模型配置与能力、最小请求测试、真实验证状态、消耗与诊断。
4. **素材库**：生图入口、角色场景绑定、参考版本、修改副本、审核和影响范围。
5. **分镜与原版制作**：标签专属分析产物、关键帧、视频生成、自动阶段进度、局部修复、时间标注和发布版本。
6. **互动放映室**：选择真实原版，任意暂停改写；展示必要进度，不暴露未播放剧情和未来反转。
7. **工作流记录**：父子阶段、输入版本、审核等待、重试与恢复、模型实际用量。

继续提供直接在三个终端运行前端、后端、Worker 的命令，按照用户偏好，不要求使用封装启动脚本。

## 9. 开发顺序与每阶段验收

| 阶段 | 工作 | 完成标准 |
|---|---|---|
| A：多故事基础与公开接口 | 数据迁移、列表详情缓存、原始标签、作者来源、完整性检查、移除固定故事引用 | 页面真实展示赛方内容；不同故事会话互不串状态；来源缺失/失败有真实提示 |
| B：模型接入基础 | 模型档案、能力适配、密钥配置、真实测试入口、用量与错误处理 | LLM 返回并通过结构化检查；生图产生可打开图片；生视频产生可播放片段；均保留真实请求证据 |
| C：导演模板与路由 | 故事分析、五类模板、标签组合、依据解释、版本化导演计划 | 两类故事执行的分析、分镜和检查步骤确实不同；未知标签和矛盾标签有兜底 |
| D：真实原版生产 | 角色场景生图、关键帧、素材绑定、分镜视频、审核与音轨字幕合成 | 至少一部赛方故事的可用原版片段通过审核发布，不混入示意素材冒充真实生成 |
| E：真实互动回归 | 暂停帧、故事级状态、同导演方案过渡、视频生成与兼容接入 | 在非预设时间改写并真实生成过渡；在生成过渡中再次改写；历史与视听连续 |
| F：差异与可靠性验证 | 第二类故事、回归测试、成本记录、直接命令运行说明 | 不同标签流程得到真实样片；故障恢复与额度控制通过；交付真实验证清单 |

顺序为 A → B → C → D → E → F。缺少模型配置时，A、B 的界面/适配层与 C 的模板/测试可继续；依赖真实结果的阶段不能标记完成。

## 10. 必须通过的验收场景

### 三类真实模型
- LLM 用赛方选定文本产出可解析的故事与分镜数据，不靠写死模板冒充模型回答。
- 生图返回新图片并落盘，审核后的版本实际传给视频任务。
- 视频通过该供应商真实接口生成、查询、下载、播放；文生视频和图生视频分别验证。
- 不支持的参数在提交前被阻止或明确适配；连接错误、余额不足和超时显示真实原因。

### 标签导演
- 对两类正文可用故事分别产生导演计划与真实样片，记录标签、选择依据、执行步骤和结果。
- 至少覆盖一个多标签混合案例，验证配角标签不会覆盖主要叙事事实。
- 未知/空标签能进入基础流程，HE/BE 标签不会凭空改写作者结局。
- 模板版本变化不影响已发布原版和旧分支。

### 互动与审核
- 原版中任意非整数时刻、对白中和动作中均可发起改写。
- 过渡片段中再次改写，旧任务不覆盖新播放列表。
- 已发生事实与人物记忆保留；桥接分别通过逻辑、视觉和时间检查。
- 不完整正文不会被冒充完整作者结局；不可回归的修改不被强行接受。
- 审核退回只重做相关部分；批准后后台自动继续。

### 运行可靠性
- 重启 Worker 能恢复已知任务；不确定的收费提交不重复发送。
- 配额限制在并发任务下有效；取消流程阻止尚未提交的任务。
- 原文、凭证与素材按范围处理；密钥不出现在前端、日志、导出或仓库。
- 本地界面、三终端命令、数据库迁移与至少一组真实端到端记录可复现。

## 11. 审核项与实施时需要用户提供的信息

请审核本计划的功能范围和阶段顺序，尤其是：
1. 是否采用“五类主流程＋标签修饰模块”，首轮实测两类故事的范围。
2. 是否保留原版素材/成片审核，读者分支按自动检查结果决定是否人工介入。
3. 是否接受正文不足时先制作节选样片，取得可信主线后再启用固定结局回归。

实施真实联调前需要：
- LLM、生图、生视频各自的供应商、官方接口文档/协议、模型 ID 与区域；不默认使用现有方舟适配器就一定适合你的账号。
- 密钥由用户本地填写；不在计划或聊天中收集完整密钥。
- 用户确认的首轮总预算或调用数/视频秒数上限，以及是否允许自动重试产生的额外调用。
- 从实际列表选择两个不同流程的故事，并确认正文与结局的可用范围。可由开发阶段先列出候选，再让用户选择。

这里等待审核是因为用户本轮明确要求“加入计划，供我审核”，不是 Skill 额外施加的审批要求。公开故事接口已经可用，不需要先安装 CLI 或申请其密钥。本版不包含自动报名、OAuth 登录、公开部署或作品提交；需要时另行安排。

### 2026-09-12 故事 API 接入验证

已通过应用适配器读取真实列表 20 篇及一篇详情（正文 3000 字，完整性未知）。浏览器确认故事库、标签筛选及查看故事入口可用。38 项离线测试及前端类型检查通过。无需知乎密钥，不调用收费生成模型。导入保存为独立 story_source 记录，避免混入旧版固定演示播放逻辑；导演和分镜工作台仍需后续开发。

### 脑洞线路第一版（用户确认其他标签后续开发）

已实现三阶段 DeepSeek 编排：导演阐述、4–8 镜短场景分镜、独立文本预审。已保存且包含脑洞标签的故事可进入专用工作台；支持修改后重审、人工批准、逐镜生成关键帧，保留导演版本与单镜素材时长。规范见 docs/brainstorm-directing.md。42 项离线测试通过，前端类型检查通过。本轮未调用真实收费模型，不能将离线验收当作真实导演输出质量验收。完整故事批量生产、音轨字幕剪辑、资产视觉一致性和任意暂停改写整合仍待开发。

### 计价与额度机制变更（以本条为准）

根据用户要求移除 PAID_CALL_LIMIT 和所有提交次数门槛；旧配置即使存在也不再限制调用。保留模型连接开关及用户显式生成操作。新增全站右下角常驻、可折叠用量面板，统计供应商返回的文本 token（含缓存命中）、成功图片数和保存视频时长。人民币单价由用户按实际账号与模型填写，请求提交时保存单价快照，修改只影响后续请求；视频当前按素材秒数估算，不等于供应商账单。历史或异常请求缺失用量/价格显示待核算，不伪造为零费用。45 项测试、前端类型检查通过，浏览器确认面板与单价输入可见。本轮无收费模型请求。

### 读者优先与制作执行台改造

默认首页改为独立读者空间，创作工具从右上角进入；用量面板留在创作空间。导演任务增加阶段状态、等待时长和阶段完成记录；既有产物折叠，提供明确的审核下一步。分镜批准后在同页制作卡片生成关键帧、审核、生成视频、预览与选取播放区间，全部审片后发布到本地读者书架。发布快照锁定片段，旧版本产物不进入新版本；视频重复提交复用现有任务。51 项测试、前端类型检查通过，浏览器验证读者首页与现有导演方案的下一步提示。

读者可连续播放已发布镜头并任意暂停记录剧情想法，输入持久化为 reader_wish，但尚未生成改写视频或桥接到作者主线；界面明确标注，不将保存想法宣称为已实现剧情改写。完整读者生成分支是下一阶段重点。当前没有发布作品时显示真实空态。本轮未自动审核用户分镜，也未发起收费模型调用。
